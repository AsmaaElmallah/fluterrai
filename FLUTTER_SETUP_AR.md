# تطبيق AlzCare - دليل التثبيت والتشغيل 🏥

## نظرة عامة 📱

تطبيق شامل لرعاية مرضى الزهايمر مبني بتقنية Flutter، يوفر ثلاث واجهات منفصلة:
- **واجهة المريض**: أنشطة تذكيرية، تتبع موقع، محادثة مع الطبيب
- **واجهة الطبيب**: إدارة المرضى، الأنشطة، النصائح، التتبع المباشر
- **واجهة العائلة**: متابعة الحالة، التتبع، المحادثة، النصائح

## المميزات الرئيسية ✨

### واجهة المريض 🧠
- ✅ لوحة تحكم شخصية مع الإحصائيات
- ✅ أنشطة ذاكرة تفاعلية
- ✅ تتبع الموقع المباشر مع تنبيهات الطوارئ
- ✅ محادثة مباشرة مع الطبيب
- ✅ ملف شخصي كامل

### واجهة الطبيب 👨‍⚕️
- ✅ لوحة تحكم شاملة للمرضى
- ✅ مكتبة نصائح وموارد (كيفية التعامل، مجموعات الدعم، نصائح للمقدمين)
- ✅ إدارة الأنشطة والتذكيرات
- ✅ تتبع مباشر متعدد المرضى
- ✅ تحديد مناطق الأمان
- ✅ نظام محادثة شامل
- ✅ ملف طبيب مهني

### واجهة العائلة 👨‍👩‍👧
- ✅ متابعة حالة المريض الفورية
- ✅ تتبع مباشر مع 3 أوضاع:
  - 📍 التتبع المباشر
  - 🛡️ مناطق الأمان
  - 📜 سجل الأماكن المزارة
- ✅ الحصول على اتجاهات للمريض
- ✅ جدول المواعيد والأنشطة
- ✅ نصائح يومية لمقدمي الرعاية
- ✅ محادثة مع المريض
- ✅ اتصال سريع بالطبيب

## المتطلبات 📋

### متطلبات النظام
- Flutter SDK 3.0.0 أو أحدث
- Dart SDK 3.0.0 أو أحدث
- Android Studio / VS Code
- محاكي Android أو جهاز حقيقي
- iOS Simulator / جهاز iPhone (للتطوير على iOS)

### التحقق من التثبيت
```bash
flutter doctor
```

## التثبيت والإعداد 🚀

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd alzcare/flutter
```

### 2. تثبيت الحزم
```bash
flutter pub get
```

### 3. تشغيل التطبيق
```bash
# على محاكي Android
flutter run

# على محاكي iOS (Mac فقط)
flutter run -d ios

# على متصفح Chrome (للاختبار)
flutter run -d chrome
```

## بنية المشروع 📂

```
flutter/
├── lib/
│   ├── main.dart                    # نقطة الدخول الرئيسية
│   ├── theme/
│   │   └── app_theme.dart           # الألوان والثيمات
│   ├── screens/
│   │   ├── role_selection_screen.dart  # شاشة اختيار الدور
│   │   ├── patient/                 # واجهة المريض
│   │   │   ├── patient_main_screen.dart
│   │   │   ├── patient_dashboard.dart
│   │   │   ├── memory_activities_screen.dart
│   │   │   ├── live_tracking_screen.dart
│   │   │   ├── chat_with_doctor_screen.dart
│   │   │   └── patient_profile_screen.dart
│   │   ├── doctor/                  # واجهة الطبيب
│   │   │   ├── doctor_main_screen.dart
│   │   │   ├── doctor_dashboard.dart
│   │   │   ├── doctor_advice_screen.dart
│   │   │   ├── doctor_activities_screen.dart
│   │   │   ├── doctor_tracking_screen.dart
│   │   │   ├── doctor_chat_screen.dart
│   │   │   └── doctor_profile_screen.dart
│   │   └── family/                  # واجهة العائلة
│   │       ├── family_main_screen.dart
│   │       ├── family_dashboard.dart
│   │       ├── family_tracking_screen.dart
│   │       ├── family_chat_screen.dart
│   │       └── family_profile_screen.dart
│   └── widgets/
│       ├── stat_card.dart           # بطاقة إحصائيات
│       └── progress_item.dart       # مؤشر تقدم
└── pubspec.yaml                     # ملف التبعيات
```

## الحزم المستخدمة 📦

```yaml
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1              # إدارة الحالة
  go_router: ^12.1.3            # التنقل
  http: ^1.1.2                  # طلبات API
  shared_preferences: ^2.2.2    # التخزين المحلي
  google_maps_flutter: ^2.5.0  # الخرائط
  geolocator: ^10.1.0           # تحديد الموقع
  fl_chart: ^0.65.0             # الرسوم البيانية
  cached_network_image: ^3.3.1 # الصور
  font_awesome_flutter: ^10.6.0 # الأيقونات
```

## دليل التطوير 💻

### إضافة شاشة جديدة
1. أنشئ ملف في المجلد المناسب (`patient/`, `doctor/`, `family/`)
2. استخدم `StatelessWidget` أو `StatefulWidget`
3. استخدم ثيم التطبيق من `AppTheme`
4. أضف الشاشة إلى قائمة الشاشات في الـ main screen

### استخدام الألوان
```dart
// الألوان الأساسية
AppTheme.tealPrimary    // #14B8A6
AppTheme.tealDark       // #0D9488
AppTheme.cyanPrimary    // #06B6D4
AppTheme.cyanLight      // #22D3EE

// الخلفيات
AppTheme.teal50         // خلفية فاتحة
AppTheme.tealGradient   // تدرج لوني
AppTheme.lightGradient  // خلفية الشاشات
```

### إنشاء بطاقة مخصصة
```dart
Card(
  child: Padding(
    padding: const EdgeInsets.all(20),
    child: // محتواك هنا
  ),
)
```

## ميزات التتبع المحسّنة 🗺️

### التتبع المباشر
- عرض الموقع الحالي في الوقت الفعلي
- مؤشر حالة الأمان (داخل/خارج المنطقة الآمنة)
- تحديث تلقائي للموقع
- زر الحصول على الاتجاهات

### مناطق الأمان
- إضافة مناطق آمنة متعددة
- تحديد نطاق (Radius) لكل منطقة
- تفعيل/تعطيل المناطق
- تنبيهات عند الخروج من المنطقة الآمنة

### سجل الأماكن
- عرض الأماكن المزارة مؤخراً
- مدة البقاء في كل مكان
- وقت آخر زيارة
- أيقونات مميزة لكل نوع مكان

## ميزات النصائح والدعم 📚

### للأطباء ومقدمي الرعاية
1. **كيفية التعامل مع المواقف**
   - فهم مراحل فقدان الذاكرة
   - التعامل مع Sundown Syndrome
   - استراتيجيات التواصل

2. **مجموعات الدعم**
   - مجموعات محلية
   - مجتمعات الإنترنت
   - ورش عمل وندوات

3. **نصائح لمقدمي الرعاية**
   - العناية بالنفس
   - إدارة الإجهاد
   - نصائح يومية

## إضافة ميزات خلفية (Backend) 🔧

### استخدام Firebase (مقترح)
```bash
# تثبيت حزم Firebase
flutter pub add firebase_core
flutter pub add firebase_auth
flutter pub add cloud_firestore
```

### استخدام REST API
```dart
import 'package:http/http.dart' as http;

Future<void> fetchData() async {
  final response = await http.get(
    Uri.parse('https://api.example.com/data'),
  );
  // معالجة البيانات
}
```

## الاختبار 🧪

### تشغيل الاختبارات
```bash
flutter test
```

### اختبار على أجهزة مختلفة
```bash
# عرض الأجهزة المتاحة
flutter devices

# تشغيل على جهاز محدد
flutter run -d <device-id>
```

## البناء للإنتاج 📦

### Android APK
```bash
flutter build apk --release
```

### Android App Bundle
```bash
flutter build appbundle --release
```

### iOS
```bash
flutter build ios --release
```

## حل المشاكل الشائعة 🔍

### مشكلة: Packages not found
```bash
flutter clean
flutter pub get
```

### مشكلة: Build failed
```bash
flutter doctor
# اتبع التعليمات لإصلاح المشاكل
```

### مشكلة: Hot reload لا يعمل
```bash
# أعد تشغيل التطبيق
r  # في terminal
```

## التصميم والألوان 🎨

التطبيق يستخدم جمالية الرعاية الصحية الحديثة:
- **الألوان الأساسية**: Teal (#14b8a6) و Cyan (#06b6d4)
- **الخلفيات**: بيضاء نقية
- **البطاقات**: زوايا مستديرة (16px)
- **الظلال**: ناعمة وخفيفة
- **الخطوط**: نظام افتراضي واضح

## الموارد الإضافية 📖

- [Flutter Documentation](https://docs.flutter.dev)
- [Dart Language Tour](https://dart.dev/guides/language/language-tour)
- [Flutter Cookbook](https://docs.flutter.dev/cookbook)
- [Material Design Guidelines](https://material.io/design)

## المساهمة 🤝

نرحب بالمساهمات! يرجى:
1. عمل Fork للمشروع
2. إنشاء Branch جديد
3. إجراء التغييرات
4. إرسال Pull Request

## الترخيص 📄

هذا المشروع مرخص تحت رخصة MIT.

## التواصل 📧

للاستفسارات والدعم، يرجى فتح Issue في المشروع.

---

**ملاحظة**: هذا تطبيق تعليمي/تجريبي. للاستخدام في بيئة إنتاجية حقيقية، يجب:
- إضافة نظام مصادقة آمن
- ربط قاعدة بيانات حقيقية
- تطبيق معايير HIPAA للخصوصية الطبية
- اختبار شامل للأمان والأداء
- الحصول على الموافقات الطبية المطلوبة

**تحذير**: لا تستخدم هذا التطبيق لجمع معلومات شخصية حساسة (PII) أو بيانات طبية حقيقية بدون تطبيق معايير الأمان والخصوصية المناسبة.
